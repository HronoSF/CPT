gtest = dependency('gtest', main: true)
test(
    'TurnOnTest',
    executable(
        'TurnOnTest',
        sources: ['TurnOnTest.cpp'],
        include_directories: src,
        dependencies: [gtest]
    )
)